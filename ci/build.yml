---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: nitram509/debian-oraclejdk8-maven-cmake-gcc-gnueabihf
    tag: latest

inputs:
  - name: source-code

outputs:
  - name: build-output

run:
  path: source-code/ci/build.sh
